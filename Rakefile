# frozen_string_literal: true

require "yaml"
require "sqlite3"
require "bundler/gem_tasks"
require "bundler/setup"
require "standard/rake"
require "mystery_maker"

Rake.add_rakelib("lib/tasks")

task default: %i[standard]
